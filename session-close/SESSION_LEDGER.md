# SESSION_LEDGER.md
**Stock Monitor + HDB Analyser — Session History**
One row per day: what was built, what was learned, what changed how we work.
Full narrative for each day lives in the Day N Summary in Git.

---

| Day | Built | Learned | Lessons |
|---|---|---|---|
| 1 | Dev environment, hello_claude.py | API auth, messages endpoint anatomy | Environment discipline first — venv, gitignore, .env |
| 2 | yfinance ETL for ticker universe | Data acquisition vs reasoning separation | Real market data is messy — handle it at the edge |
| 3 | Structured JSON between pipeline stages | LLM output is probabilistic text, not guaranteed JSON | Never trust model output format — validate before use |
| 4 | stock_monitor v3 with portfolio context, Translator chained. HDB scaffolded. | System prompt vs user message; chained call pattern | Don't dilute a persona to fix its audience problem — add a second persona |
| 5 | HDB pipeline end-to-end: data.gov.sg fetch, section-by-section analyst, extract_json() | Anchoring failure; per-section token budgets | Confident wrong numbers on a large-ticket purchase are worse than no numbers |
| 6 | All constants to config.py, ANALYST_SECTION_DEPENDENCIES | Context economics; response.usage for cost accounting | Magic numbers are silent bugs |
| 7 | shared/utils.py, HDB persona rewrite | Prompt as a contract; word limits force prioritisation | DRY across projects; pay technical debt before adding complexity |
| 8 | call_llm() wrapper, SQLite prices.db 6 tables | Abstracting model interface so swaps are config changes | Partial writes must be impossible — context-manager commits with rollback |
| 9 | 10-table schema, six-agent routing, truncation guard, market_history backfill, cost tracking | Adversarial multi-agent design; kill triggers as pre-commitments | A 90% complete argument is still signal — flag truncation, never discard |
| 10 | Full three-stage pipeline live: Stage 1→Contrarian→Meta-Agent. Data package builder, kill trigger checker, persona_calls, VIX classifier | Portfolio-level synthesis vs per-ticker reasoning; compress() | First live run flagged G3B.SI as false diversifier — system produced genuine insight day one |
| 11 | thesis_drafts/reviews tables, thesis_overrides.json, staleness flag, 37 agent fixtures captured | Fixture infrastructure as foundation of prompt tuning | Hard stop over silent fallback |
| 12 | Codebase map, check_portfolio_correlations(), Gmail SMTP alerts, score_persona_call_outcomes() stub | Correlation math confirmed what Meta-Agent flagged qualitatively | A codebase map saved more time than it cost |
| 13 | Warning audit, format_warning() formatter, +5/+20 trading-day scoring, living documents seeded | format_warning() pattern; two horizons because correct at +5 ≠ correct at +20 | Seed living documents from actual code — two discrepancies found immediately |
| 14 | check_stuck_runs(), run_pipeline() crash wrapper, Task Scheduler configured | Two-layer scheduling: OS owns clock, Python owns resilience | Start in field mandatory in Task Scheduler — missing it causes 0x2 error |
| 15 | shared/utils.py made config-free, project wrapper pattern (sm_call_llm etc) | sys.modules caching is the silent killer of shared imports | Shared modules must never import project config — enforce at every code review |
| 16 | Sovereign SLM tier: Ollama, _call_ollama(), shadow cost tracking, Feed Relevance Engine designed | Model size does not predict output quality; MoE architecture | Cannot benchmark at proxy sizes |
| 17 | Feed Stage 1: 22 RSS sources, feeds table, feeds.py, feeds_audit.py | RSS is a standardised XML file; bozo flag is not binary failure | Feed URL rot is real — feeds_audit.py should run monthly |
| 18 | HDB buyer_type as first-class field, BUYER_TYPES registry, dual mandate output schema, translator rewritten | buyer_type as routing key routes to an entire rule set | Prompt vagueness is the root cause of output drift |
| 19 | prompt_text column to llm_calls (OQ-004 Gap 1 closed), num_ctx truncation bug fixed, phi4-mini confirmed non-viable Stage 3 xxl | num_ctx is total budget (input+output combined), not output headroom | Honest benchmark failure is strictly more valuable than dishonest success |
| 20 | Re-baseline: AI concept ranking, goal-state criteria, agent roster, forward queue, Master Plan v1.2, five PK documents as markdown | Drift traced to binding content to day numbers — forward queue holds no day numbers | Precedence rule must be applied at first message of every session |
| 21 | prompts/schemas.py (six Pydantic schemas), --schema flag, schema-guided decoding, decode_mode column, full 4×4 benchmark grid | Schema-guided decoding is categorically different from prompt-only — FSM masks invalid tokens at generation time | Schema enforces presence and type, not quality of content |
| 22 | Day 21 commit (8e34500). Four harness fixes: raw_response 2000→5000, EXPECTED_TICKER injection, ticker_mismatch check, ticker_mismatch column. Four new models pulled. | Missing ticker = model ignored direct instruction; ALTER TABLE appends physically; context window marketing ≠ effective limits | Fix the ruler before measuring new things with it |
| 23 | Full benchmark grid all 8 models (schema+prompt-only, small/medium/xl/xxl). Re-runs at 2400 tokens. Mismatch column in display. read_responses.py. | magistral schema never engages; gemma4:26b collapse is context-size/budget-dependent; content drift is distinct from field drift | Read raw responses before routing conclusions — flags hide content quality |
| 24 | Contrarian ticker grounding (analyst_persona.py + load_stage1_outputs()). Item A prompt reorder (build_data_package + build_synthetic_prompt). SLM routing LOCKED. | Grounding necessary but not sufficient — only qwen3.6 (thinking mode) resisted drift. Lost-in-middle root cause is positional encoding bias. KG is permanent solution to all five systemic risks. | Root-cause input shape before blaming the model — Contrarian drift was information architecture failure, not model failure |
| 25 | schemas.py v1.1 (reasoning_raw_text first-field, raw_data_citations soft-enforced). Found+fixed live bug: sm_call_llm() compared nested SLM_STAGE_MODELS dict to string "heavy" — always False, every SLM call silently fell through to phi4-mini regardless of stage. Fixed with primary→fallback resolution, per-stage max_tokens override. Stage 2 fallback set None (sovereignty stance — no cloud safety net). Translator SLM-routed (untested). Dead two-tier SLM_MODE system deleted. One-ticker live SLM test confirmed correct routing. | Bug was dormant since Day 24 — USE_SLM stayed False throughout benchmark work, so it never fired in a live run. schemas.py v1.1's added fields likely raised the real token floor — 3 of 7 calls truncated on first live test at old Day 21-24 budgets. | A locked routing table in config means nothing if the consumer code doesn't read it correctly — verify wiring with a real run, not just a config diff |
| 26 | `llm_calls.ticker` column added (ALTER TABLE + database.py + write_llm_call() + 4 call sites in stock_monitor.py). `load_captured_prompt()` fixed: ticker filter + ORDER BY rowid DESC. Stage 1/2/Translator SLM ceilings raised to 6000 primary. json-repair fallback in extract_json(). TARGET TICKER framing added to run_stage1_agent() call site. Grounding rules added to Stage 1 prompts (premature — cleanup logged). Full 8-ticker live SLM run clean: 0 errors, 0 warnings, 3 retries resolved. | Root cause of ticker drift: benchmark always loaded highest-token historical row (G3B.SI), not the ticker under test — deterministic code bug, not LLM anchoring. json-repair handles missing string-open quotes cleanly. Grounding rules were added before diagnosing root cause. | Diagnose before patching. A benchmark loading wrong data is indistinguishable from model failure until you read the SQL. |

---

*Full narrative per day in Day N Summary files (Git). Only most recent Day Summary in Project Knowledge.*
